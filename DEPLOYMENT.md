# نشر Goat Journey

مرجع هذا الإصدار هو [SEO-DEPLOYMENT.md](SEO-DEPLOYMENT.md)، ويشمل إعداد Supabase، صلاحيات الإدارة، البناء والاختبارات والفهرسة.

لا يوجد حساب إدارة افتراضي أو كلمة مرور منشورة. الدخول يتطلب حساب Supabase Auth فعلياً مع `app_metadata.role = "admin"` من مصدر إداري موثوق. لا تعتمد على البريد أو `profiles.role` وحدهما للحماية.

1. تأكد من مشروع Supabase الذي تستخدمه نسخة Goat Journey الحالية، واضبط القيم من `.env.local.example`.
2. جهّز حساب الإدارة وراجع سياسات قاعدة البيانات قبل نشر حماية الإدارة الجديدة.
3. اختبر البناء وملف `scripts/verify-seo.mjs`، ثم انشر الفرع عبر مشروع الاستضافة الحالي.
4. أعد فحص الموقع الحي وأرسل خريطة الموقع عبر حسابات البحث التي تملكها.

إصلاح قاعدة البيانات الموجودة هو `fix-profiles-rls.sql` بعد المراجعة على المشروع الصحيح. ملفات إنشاء الجداول والبيانات الأولية لا تُشغّل على الإنتاج كخطوة تحديث.
